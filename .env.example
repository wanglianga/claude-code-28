# 宿主机发布端口（compose 中映射为 ${CC_PUBLISH_PORT}:3000）
CC_PUBLISH_PORT=3028

# 数据库连接（compose 内部网络，无需发布到宿主）
POSTGRES_USER=art
POSTGRES_PASSWORD=artclass
POSTGRES_DB=artclass

# 应用配置（容器内）
PORT=3000
DATABASE_URL=postgres://art:artclass@db:5432/artclass
